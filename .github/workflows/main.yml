name: Training Workflow

on:
  push:
    branches:
      - main

permissions:
  contents: write

jobs:
  train-and-commit:
    runs-on: ubuntu-latest

    steps:
    - name: Checkout code
      uses: actions/checkout@v3

    - name: Set up Python
      uses: actions/setup-python@v4
      with:
        python-version: '3.10'

    - name: Install dependencies
      run: |
        pip install mlflow pandas numpy scikit-learn matplotlib seaborn

    - name: Run MLflow Project
      run: |
        # Kita panggil langsung file py-nya untuk menghindari masalah entry_point
        cd MLProject
        mlflow run . --env-manager local --entry-point main

    - name: Commit & Push Artifacts
      run: |
        git config --global user.name 'github-actions[bot]'
        git config --global user.email 'github-actions[bot]@users.noreply.github.com'
        git add MLProject/mlruns/ || echo "Folder mlruns belum ada"
        git commit -m "Auto-commit: New training run results" || echo "No changes to commit"
        git push